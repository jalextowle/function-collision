# Solidity Function Selector Collision Checker

This tool will find all "reasonable" function selector collisions 
for a given function and a provided ABI

# Testing

Run the unit tests by running:

```
make test
```
